# --- Web framework ---
fastapi>=0.115.0,<0.120.0
uvicorn[standard]>=0.32.0,<0.36.0
python-multipart>=0.0.18          # multipart/form-data (ZIP upload)
slowapi>=0.1.9                    # rate limiting (replaces flask-limiter)

# --- Config & validation ---
pydantic>=2.9.0,<3.0.0
pydantic-settings>=2.6.0,<3.0.0
python-dotenv>=1.0.1

# --- HTTP client (GitHub tarball fetch, OSV.dev queries) ---
httpx>=0.28.0,<0.29.0

# --- Persistence ---
SQLAlchemy[asyncio]>=2.0.36,<2.1.0
asyncpg>=0.30.0                   # Postgres driver (production)
aiosqlite>=0.20.0                 # SQLite driver (local dev without Postgres)
alembic>=1.14.0

# --- Auth ---
# Both were already installed in the local venv as Flask-era leftovers, so
# omitting them here worked in development and would only have failed on deploy.
cryptography>=44.0.0              # Fernet, for GitHub tokens at rest
itsdangerous>=2.2.0               # signed, stateless OAuth `state`

# --- AI provider ---
google-genai>=1.0.0               # current unified SDK; replaces google-generativeai 0.3.0

# --- Tests ---
pytest>=8.3.0
pytest-asyncio>=0.24.0
respx>=0.22.0                     # mock httpx (OSV / GitHub / LLM) in tests
