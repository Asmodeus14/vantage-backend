# Development-only. Deliberately not in requirements.txt: the deployed instance
# installs that on every deploy, and a linter is dead weight in a free-tier
# container that is already tight on install time and memory.
-r requirements.txt

ruff>=0.16

# Validates exported SARIF against the OASIS 2.1.0 schema in tests. The
# schema is vendored at tests/fixtures/ so the suite stays offline.
jsonschema>=4
